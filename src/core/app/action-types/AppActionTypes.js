const namespace = 'app'
const REQUEST_ME = `${namespace}/request_me`
const REQUEST_USER = `${namespace}/request_user`
const REQUEST_CATEGORIES = `${namespace}/request_categories`
const REQUEST_PRODUCTS = `${namespace}/request_products`
const ADD_CARD = `${namespace}/add_card`
const SUB_CARD = `${namespace}/sub_card`
const SET_SEARCH_TEXT = `${namespace}/set_search_text`

export default {
	ADD_CARD,
	SUB_CARD,
	REQUEST_ME,
	REQUEST_USER,
	REQUEST_CATEGORIES,
	REQUEST_PRODUCTS,
	SET_SEARCH_TEXT,
}
